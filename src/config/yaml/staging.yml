# Server configurations
server:
  port: "8080"

# Database credentials
database:
  uri: "mongodb://indoquran:indoquran@localhost:27017/?serverSelectionTimeoutMS=5000&connectTimeoutMS=10000&authSource=indoquran&authMechanism=SCRAM-SHA-256"
  # uri: "mongodb+srv://nova:admin123@cluster0.df2xr.mongodb.net/indoquran?retryWrites=true&w=majority"
  dbname: "indoquran"

# Redis configurations
cache:
  uri: "redis://localhost:6379/0"
  pass: ""
  ttl: 12 # time to leave in hour value
